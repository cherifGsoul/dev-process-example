apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "course-enrollement.fullname" . }}-test-connection"
  labels:
    {{- include "course-enrollement.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "course-enrollement.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
