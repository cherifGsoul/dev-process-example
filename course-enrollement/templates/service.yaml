apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-enrollement.fullname" . }}
  labels:
    {{- include "course-enrollement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "course-enrollement.selectorLabels" . | nindent 4 }}
